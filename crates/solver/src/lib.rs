// Layout composition crate (stub). Re-exports the template library it builds on.
//
// The recipe database and production-chain calculator are not yet implemented —
// their modules were declared before any code existed and have been removed so
// the workspace compiles. See the backlog for the planned solver phases.
pub use factorio_templates;
