[package]
name = "factorio-templates"
version.workspace = true
edition.workspace = true

[dependencies]
factorio-grid = { path = "../grid" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
